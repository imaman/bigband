#!/bin/bash

set -e

yarn test

REPORT_FILE=$(mktemp)
# pushd modules/septima-lang
npx monocrate modules/septima-lang --bump=minor --report=$REPORT_FILE

V=$(head -n 1 "$REPORT_FILE")

T="published@$V"
echo tagging "$T"
git tag "$T"
git push --tags
